{
  "version": 1,
  "hooks": {
    "beforeShellExecution": [
      {
        "command": ".cursor/hooks/allow-apple-deploy.sh",
        "matcher": "apple-upload/scripts/deploy\\.sh|android-upload/scripts/deploy\\.sh|npm run deploy:ios:prod|npm run deploy:android:prod|eas build --platform (ios|android) --profile production|eas submit --platform (ios|android) --profile production"
      }
    ]
  }
}
